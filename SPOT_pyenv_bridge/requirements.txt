# Numpy-only inference of the trained MLP -- NO torch / SB3 / conda needed.
# The network forward pass is plain matrix math; weights live in spot_policy.mat.
# Works on a plain python.org install (sidesteps the conda DLL issues).
numpy
scipy
